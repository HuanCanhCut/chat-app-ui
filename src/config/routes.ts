interface Routes {
    home: string
    user: string
    auth: string
    message: string
    stories: string
    create_story: string
}

export const routes: Routes = {
    home: '/',
    user: '/user',
    auth: '/auth',
    message: '/message',
    stories: '/stories',
    create_story: '/stories/create',
}
