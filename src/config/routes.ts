interface Routes {
    home: string
    user: string
    auth: string
    message: string
}

export const routes: Routes = {
    home: '/',
    user: '/user',
    auth: '/auth',
    message: '/message',
}
